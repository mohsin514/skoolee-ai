import { StudentRouteSkeleton } from "@/components/student/student-components";

// Rendered inside StudentShell, so only the page card is standing in.
export default function StudentLoading() {
  return <StudentRouteSkeleton />;
}
